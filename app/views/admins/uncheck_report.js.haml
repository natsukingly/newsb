$('.notice_bar.for_js').css("display", "flex").delay(4000).fadeOut('slow'); 
$('.notice_bar.for_js').text("You changed the report's status as [NOT YET]"); 
$(".report_#{@report.id}").find("span.status").text("STATUS: NOT YET");
$(".report_#{@report.id}").find(".report_menu").html("#{escape_javascript( link_to 'CHECK', check_report_admin_path(@report.id), remote: true, class: 'check_report_btn report_btn', method: :post)}");	