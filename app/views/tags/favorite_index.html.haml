.sub_function_wrapper.tags
	.sub_function_main_section
		.sub_function_nav
			%h2
				TAGS
			
			.nav_options	
				=link_to "TRENDING", tags_path
				=link_to favorite_index_tags_path, class: "active" do
					FAVORITE・#{current_user.favorite_tags.count}

		#main_contents
			=render @tags
	
	.sub_function_sidebar
		%h2
			NEW USERS
		.new_users_section
			-unless @new_users.empty?
				= render partial: "users/card", collection: @new_users, as: :user
