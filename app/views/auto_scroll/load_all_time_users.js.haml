$('.scroll_btn.users').before('#{ escape_javascript(render @loaded_users)}');
$('.scroll_btn.users').html("#{t('scroll.load')}");
$('.scroll_btn.users').removeClass('loading');
-if @loaded_users.empty? || @loaded_users.count < 30
    $('.scroll_btn.users').html("#{t('scroll.no_more')}");
    $('.scroll_btn.users').addClass('stop_loading');
    
loading = false;
