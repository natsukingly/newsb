$(".comments").html('<%= escape_javascript(render @post.comments)%>');
$(".comment_count").text("<%= @post.comments.count %>");
$("#comment_form").html('<%= escape_javascript(render("comments/form", post: @post, comment: @new_comment ))%>'); 