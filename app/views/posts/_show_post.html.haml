#show_post_block.clearfix
  .cover{style: "background-image: url(#{post.article_image})"}
    =image_tag user.image.profile_md.url, class: "img-circle"
  %div{class: "profile user_#{user.id}"}
    %h2= user.name 
    %p= user.shoulder_name
    .about
      %p
        =post.content