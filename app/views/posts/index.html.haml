%p#notice= notice
%h1 Posts
%table
  %thead
    %tr
      %th User
      %th Title
      %th Content
      %th Article title
      %th Article image
      %th Article url
      %th{:colspan => "3"}
  %tbody
    - @posts.each do |post|
      %tr
        %td= post.user_id
        %td= post.title
        %td= post.content
        %td= post.article_title
        %td= post.article_image
        %td= post.article_url
        %td= link_to 'Show', post
        %td= link_to 'Edit', edit_post_path(post)
        %td= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Post', new_post_path
